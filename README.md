This project exemplifies a PHP implementation of a Double Linked List.  Unit tests are run with PHPUnit by executing the script below:
```
$ php lib/phpunit.phar --bootstrap src/autoload.php tests
```
